<%= simple_form_for @news, :html => { :class => 'form-horizontal' } do |f| %>
<fieldset>
  <legend><%= controller.action_name.capitalize %> Nyhet</legend>

  <%= f.input :title %>
	<%= f.input :ingress %>
  	<%= f.input :body %>
 
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Cancel',news_index_path, :class => 'btn' %>
  </div>
</fieldset>
<% end %>

$('#tracks_container_<%= @side.id %>').append("<%= j render(:partial => '/orders/addtrack', formats: [ :html ], locals: { side: @side, track: @track, tracks_counter: @tracks_counter})  %>");

var substring = $('#add_track_<%= @side.id %>').attr('href').split("tracks_counter=")
var new_count = parseInt(substring[1]) +1;

$('#add_track_<%= @side.id %>').attr('href', substring[0] + "tracks_counter=" + new_count);

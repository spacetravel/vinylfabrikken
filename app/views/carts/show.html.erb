<div class="span9 offset1">
	<% if notice %>
	<p id="notice" ><%= notice %></p>
	<% end %>

	<div class="cart_title">Min handlekurv</div>
	 	<table class="table table-stiped">
		 	<% @cart.line_items.each do |item| %>
		 		<tr>
				 	<td class="handlekurv_line"><%= item.order.title %> </td>
				 	<td class="handlekurv_line"><%= item.order.artist %> </td>
				 	<td class="handlekurv_line"><%= item.order.quantity %> eksemplarer</td>
				 	<td class="handlekurv_line"> 5000,00 kr </td>
				 </tr>
		 	<% end %>
		 	<tr>
		 			<td colspan="2">Total pris</td>
		 			<td class="total_cell">5600,00 kr (inkl. mva)</td>
		 	</tr>
	 	</table>


	<%= button_to 'Tøm handlekurv', @cart, class: 'btn btn-warning', method: :delete, confirm: 'Er du helt sikker?' %>

</div>

